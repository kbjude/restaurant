import icon1 from './images/salad.png'
const menu = () => {
    const imageCollection = document.images
    const menuDiv = document.createElement(div);
    const menuContent = document.createTextNode(
        "Our Daily Menu has"
        "Photo one", "Photo two"
    );


    menuDiv.appendChild(menuContent);
    menuDiv.appendChild(icon1);
}